# Copy to .env and adjust (never commit .env)

PORT=3000

# Comma-separated browser origins (CORS). If unset, localhost + Netlify prod are allowed.
# CLIENT_ORIGIN=http://localhost:5173,https://influenceos-app.netlify.app
